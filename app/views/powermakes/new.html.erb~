<% if flash[:notice] %>
	<div class="notice"><%= flash[:notice] %></div>
<% end %>
<table width="100%" border="0" cellpadding="20" cellspacing="0">
	<tr>
		<td align="left" valign="top">
			<% if @user.admin %>
				<h3>New Power Make</h3>

				<% form_for(@powermake) do |f| %>
					<%= f.error_messages %>

					<p>
						<%= f.label :make %><br />
						<%= f.text_field :make %>
					</p>
					<p>
						<%= f.submit 'Create' %>
					</p>
				<% end %>

				<%= link_to 'Back', powermakes_path %>
			<% else %>
				<p>This page is for Administrators only!</p>
			<% end %>
		</td>
	</tr>
</table>
